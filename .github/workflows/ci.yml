name: ci

on:
  pull_request:
  push:
    branches:
      - main
  merge_group:
    types:
      - checks_requested

permissions:
  contents: read

env:
  OCS_SHIPPED_PACKAGE_GLOBS: >-
    examples/synthetic-service-module/connector-package.json
    reference/synthetic-service/module-package.json
    modules/*/module-package.json

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  go-test:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false
      - uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
        with:
          go-version: 1.26.8
          check-latest: false
          cache: true
      - name: Verify delivery roadmap
        run: go run ./cmd/cloudring-roadmap verify --root ./roadmap
      - name: Test minimum supported Go
        run: go test ./... -count=1
      - name: Test real offline etcd snapshot recovery
        shell: bash
        run: |
          set -euo pipefail
          version='3.6.14'
          archive="${RUNNER_TEMP}/etcd-v${version}-linux-amd64.tar.gz"
          release="${RUNNER_TEMP}/etcd-v${version}-linux-amd64"
          curl --fail --location --proto '=https' --tlsv1.2 \
            --output "${archive}" \
            "https://github.com/etcd-io/etcd/releases/download/v${version}/etcd-v${version}-linux-amd64.tar.gz"
          echo 'ffe840ff9295808e88cce2794a18a5ac87f12a5203c8314d0bf6aa119b41bac5  '"${archive}" | sha256sum --check --strict
          tar --extract --gzip --file "${archive}" --directory "${RUNNER_TEMP}"
          data_dir="${RUNNER_TEMP}/synthetic-etcd"
          snapshot="${RUNNER_TEMP}/synthetic-snapshot.db"
          loopback_host='127.0.'
          loopback_host+='0.1'
          client_url="http://${loopback_host}:2379"
          peer_url="http://${loopback_host}:2380"
          "${release}/etcd" \
            --data-dir "${data_dir}" \
            --name synthetic \
            --listen-client-urls "${client_url}" \
            --advertise-client-urls "${client_url}" \
            --listen-peer-urls "${peer_url}" \
            --initial-advertise-peer-urls "${peer_url}" \
            --initial-cluster "synthetic=${peer_url}" \
            >"${RUNNER_TEMP}/synthetic-etcd.log" 2>&1 &
          etcd_pid=$!
          trap 'kill "${etcd_pid}" 2>/dev/null || true; wait "${etcd_pid}" 2>/dev/null || true' EXIT
          for attempt in $(seq 1 60); do
            if "${release}/etcdctl" --endpoints="${client_url}" endpoint health >/dev/null 2>&1; then
              break
            fi
            if [[ "${attempt}" == 60 ]]; then
              exit 1
            fi
            sleep 1
          done
          "${release}/etcdctl" --endpoints="${client_url}" put synthetic-key synthetic-value >/dev/null
          "${release}/etcdctl" --endpoints="${client_url}" snapshot save "${snapshot}" >/dev/null
          kill "${etcd_pid}"
          wait "${etcd_pid}" || true
          trap - EXIT
          chmod 0600 "${snapshot}"
          GOTOOLCHAIN=go1.26.8 bash .github/scripts/build-release-etcdutl.sh \
            "${RUNNER_TEMP}/release-etcdutl"
          bash .github/scripts/check-release-binaries.sh "${RUNNER_TEMP}/release-etcdutl/etcdutl"
          CLOUDRING_TEST_ETCD_SNAPSHOT="${snapshot}" \
          CLOUDRING_TEST_ETCDUTL="${RUNNER_TEMP}/release-etcdutl/etcdutl" \
            go test -tags=etcdrecoveryintegration ./pkg/backup/etcdrecovery -run '^TestRealSyntheticSnapshotRecovery$' -count=1

  go-test-race:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false
      - uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
        with:
          go-version: 1.26.8
          check-latest: false
          cache: true
      - run: go test -race ./... -count=1

  postgres-integration:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    container:
      image: golang:1.26.8-bookworm@sha256:9fdc884aacc3bec89b20ffc69f4bb369c78210e3e4f600387b5128b12c199f81
    services:
      postgres:
        image: postgres:18.6-bookworm@sha256:1c59e2c3c818eaa0f0628f695b36e7c9e362d6b219b36a54a32df645cbd7e1af
        env:
          POSTGRES_HOST_AUTH_METHOD: trust
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres -d postgres"
          --health-interval 2s
          --health-timeout 5s
          --health-retries 30
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false
      - uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
        with:
          go-version: 1.26.8
          check-latest: false
          cache: true
      - name: Test transactional state and public runtime against PostgreSQL
        env:
          CLOUDRING_POSTGRES_TEST_DSN: host=postgres port=5432 user=postgres dbname=postgres sslmode=disable
        run: |
          go test ./pkg/transactionalstate -run '^TestPostgreSQLDocumentLifecycleAndConcurrency$' -count=1
          go test ./pkg/controlplane -run '^TestPostgreSQLProviderRuntimeJourney$' -count=1 -v
      - name: Test guest database directory interruption and ownership as root
        run: |
          test "$(id -u)" -eq 0
          go test ./pkg/devinstall -run 'TestGuestPostgreSQLDirectory.*AsRoot$' -count=1 -timeout=2m -v

  go-vet:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false
      - uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
        with:
          go-version: 1.26.8
          check-latest: false
          cache: true
      - run: go vet ./...

  platform-manifests:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false
      - uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
        with:
          go-version: 1.26.8
          check-latest: false
          cache: true
      - name: Verify generic platform manifests
        run: |
          go test ./internal/platformmanifest -count=1
          kubectl kustomize deploy/kubernetes/storage/csi-snapshot-api/crds >/dev/null
          kubectl kustomize deploy/kubernetes/storage/csi-snapshot-api/controller >/dev/null
          go run ./cmd/cloudring-manifestcheck --root .

  windows-go-test:
    runs-on: windows-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false
      - uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
        with:
          go-version: 1.26.8
          check-latest: false
          cache: true
      - name: Test minimum supported Go
        run: go test ./... -count=1

  go-build-module-integrity:
    runs-on: ubuntu-latest
    timeout-minutes: 40
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false
      - uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
        with:
          go-version: 1.26.8
          check-latest: false
          cache: true
      - name: Verify module graph and build
        shell: bash
        run: |
          set -euo pipefail
          go mod verify
          go mod tidy -diff
          go list -mod=readonly -m all
          go build -mod=readonly -trimpath ./...
      - uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
        with:
          go-version: 1.26.8
          check-latest: false
          cache: true
      - name: Reproduce, scan and execute the complete release bundle
        run: |
          bash .github/scripts/build-release-bundle.sh "${RUNNER_TEMP}/release-bundle"
          tar --extract --gzip \
            --file "${RUNNER_TEMP}/release-bundle/cloudring-linux-amd64.tar.gz" \
            --directory "${RUNNER_TEMP}"
          "${RUNNER_TEMP}/cloudring-linux-amd64/bin/cloudring-roadmap" verify --root ./roadmap

  ocs-validate:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false
      - uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
        with:
          go-version: 1.26.8
          check-latest: false
          cache: true
      - name: Validate every shipped connector package
        shell: bash
        run: |
          set -euo pipefail
          shopt -s nullglob
          # shellcheck disable=SC2206 # The reviewed value intentionally contains a tracked glob.
          packages=(${OCS_SHIPPED_PACKAGE_GLOBS})
          (( ${#packages[@]} >= 3 ))
          go run ./cmd/ocsctl validate "${packages[@]}"

  ocs-conformance:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false
      - uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
        with:
          go-version: 1.26.8
          check-latest: false
          cache: true
      - name: Check conformance for the same shipped connector packages
        shell: bash
        run: |
          set -euo pipefail
          shopt -s nullglob
          # shellcheck disable=SC2206 # The reviewed value intentionally contains a tracked glob.
          packages=(${OCS_SHIPPED_PACKAGE_GLOBS})
          (( ${#packages[@]} >= 3 ))
          go run ./cmd/ocsctl conformance "${packages[@]}"
      - name: Build and smoke-test the synthetic reference image
        shell: bash
        run: |
          set -euo pipefail
          image='cloudring-synthetic-service:ci'
          docker build --file ./reference/synthetic-service/Containerfile --tag "${image}" .
          docker run --rm "${image}" --mode=mock-provider --check

  docscheck:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false
      - shell: bash
        run: |
          set -euo pipefail
          for file in README.md LICENSE NOTICE CONTRIBUTING.md SECURITY.md GOVERNANCE.md CLA.md DCO.md TRADEMARKS.md; do
            test -s "$file"
          done
          test -s docs/public-boundary.md
          test -s deploy/kubernetes/secret-manager/README.md

  source-safety:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          fetch-depth: 0
          persist-credentials: false
      - uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
        with:
          go-version: 1.26.8
          check-latest: false
          cache: true
      - name: Scan complete checkout
        run: go run ./cmd/cloudring-sourcecheck scan --scope full
      - name: Scan every commit in the proposed range
        shell: bash
        run: |
          set -euo pipefail
          case "${GITHUB_EVENT_NAME}" in
            pull_request)
              base_sha="$(jq -r '.pull_request.base.sha // empty' "${GITHUB_EVENT_PATH}")"
              head_sha="$(jq -r '.pull_request.head.sha // empty' "${GITHUB_EVENT_PATH}")"
              ;;
            merge_group)
              base_sha="$(jq -r '.merge_group.base_sha // empty' "${GITHUB_EVENT_PATH}")"
              head_sha="$(jq -r '.merge_group.head_sha // empty' "${GITHUB_EVENT_PATH}")"
              ;;
            push)
              base_sha="$(jq -r '.before // empty' "${GITHUB_EVENT_PATH}")"
              head_sha="$(jq -r '.after // empty' "${GITHUB_EVENT_PATH}")"
              ;;
            *)
              echo "unsupported GitHub event for source-safety history scan: ${GITHUB_EVENT_NAME}" >&2
              exit 1
              ;;
          esac
          head_sha="${head_sha:-${GITHUB_SHA}}"
          base_sha="${base_sha:-0000000000000000000000000000000000000000}"
          go run ./cmd/cloudring-sourcecheck scan --scope pre-push --base "${base_sha}" --head "${head_sha}"
